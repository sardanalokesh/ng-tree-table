import { FirstColumnDirective } from './first-column.directive';

describe('FirstColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new FirstColumnDirective();
    expect(directive).toBeTruthy();
  });
});
